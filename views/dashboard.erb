<div class="container allPosts">
  <% @blogposts.each do |blog| %>
  <div class="row indexBlogRows">
    <div class="col-lg-12 text-center indexTitle">
      <h2>
      <strong>
      <%= blog.name %>
    </strong>
  </h2>
    </div>
    <div class="col-lg-12 text-left indexDescription">
      <h3>
     <%= blog.description %>
   </h3>
   </div>
  </div>
  <% end %>
</div>
